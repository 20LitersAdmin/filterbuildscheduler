<%= @recipient.fname %>,

Thanks for registering to build filters with 20 Liters. We're glad you've signed up!

Here are the details:
=========================
<%= render partial: 'details' %>

If you can no longer make it, <%= link_to "click here to cancel your registration.", event_registration_path(@event, @registration, user_token: @recipient.authentication_token, user_email: @recipient.email ), method: :delete %>

<% if @token %>
=========================
If you plan to build often, you might want to save time by logging in. You can finish setting up your account here:
<%= edit_password_url(@recipient, reset_password_token: @token) %> to
<% end %>


