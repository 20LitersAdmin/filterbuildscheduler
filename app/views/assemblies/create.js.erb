// clear and close the modal form
$('#assembly_modal_body').html('');
$('#edit_assembly_modal').modal('hide');

// update the DOM by replacing the entire table
$('table.edit_combination_tbl tbody').html("<%= j render partial: 'combinations/assembly_edit', collection: @combination.assemblies.ascending %>");

// clear out any old flash messages
$('.main div.alert').remove();

// fake a flash message
$('.main').prepend('<div class="alert fade in <%= @msg_type %>"><button class="close" data-dismiss="alert">x</button><%= @message %></div>');
