// clear out the modal body
$('#count_modal_body').html('cleared');
// close the modal
$('#edit_count_modal').modal('hide')

// update the collection partial:
// handeled by CountsChannel && javascripts/channels/counts_channel.js
