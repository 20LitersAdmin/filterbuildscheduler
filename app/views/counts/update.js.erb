// clear out the modal body
$('#count_modal_body').html('');
// close the modal
$('#edit_count_modal').modal('hide')

// update the partial
// this should now be handled by CountsChannel
// $("div#count_<%= @count.id %>").html("<%= j render @count %>");
