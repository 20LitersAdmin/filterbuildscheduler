// clear out the modal body
$('#count_modal_body').html('');
// close the modal
$('#edit_count_modal').modal('show')

// update the partial
// 'views/inventory/_count_container' --> views/count/_count'
$("div#count_<%= @count.id %>").html("<%= j render 'count' %>");

// trigger the count_poller
