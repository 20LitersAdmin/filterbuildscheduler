-# frozen_string_literal: true

.col-xs-8
  %h2 Inventory History
.col-xs-4.right-align
  = link_to 'Back', inventories_path, class: 'btn btn-small yellow'
.col-xs-12.col-sm-8.col-sm-offset-2.col-md-6.col-md-offset-3
  %table.table.table-striped.datatable-search
    %thead
      %tr
        %th Inventory
        %th.center-align Items counted
    %tbody
      - @inventories.each do |inventory|
        %tr
          %td{ 'data-sort': inventory.date.iso8601 }
            %strong= link_to inventory.name, inventory_path(inventory)
          %td.center-align= human_number inventory.history.size

