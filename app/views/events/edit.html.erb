<div class="container event-edit">
  <div class="page-header">
    <h1><%= @event.title %></h1>
    <% if @event.is_private %>
      <h4 class="center-align">Private Event</h4>
    <% end %>
  </div>
  <% if @event.registrations.exists? %>
    <div class="event-edit-warning">
      <h4 class="center-align alert danger">WARNING! People have registered for this event.<br>If you change the location, times, or technology, everyone registered will be notified.</h4>
    </div>
  <% end %>
  <div>
    <%= render partial: 'form' %>
  </div>
</div>
